name: Build Launcher DMG

on:
  workflow_dispatch:

jobs:
  dmg:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download build assets from release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release download build-source-v1 \
            --repo ${{ github.repository }} \
            --pattern "*.zip" \
            --dir downloads

      - name: Unzip Launcher macOS build
        run: |
          mkdir -p extracted
          unzip -q "downloads/NoblesLauncher-macOS.zip" -d extracted
          ls -la extracted

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create DMG
        run: |
          mkdir -p dist
          create-dmg \
            --volname "Nobles Launcher" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --app-drop-link 600 185 \
            "dist/Nobles Launcher.dmg" \
            "extracted/Nobles Launcher.app"

      - name: Zip DMG
        run: |
          mkdir -p dist-zip
          zip -j "dist-zip/NoblesLauncher-dmg.zip" "dist/Nobles Launcher.dmg"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nobles-Launcher-dmg
          path: "dist-zip/NoblesLauncher-dmg.zip"
