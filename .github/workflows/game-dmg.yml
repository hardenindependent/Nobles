name: Build Game DMG

on:
  workflow_dispatch:

jobs:
  dmg:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download build assets from release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release download build-source-v1 \
            --repo ${{ github.repository }} \
            --pattern "*.zip" \
            --dir downloads

      - name: Unzip Game macOS build
        run: |
          mkdir -p extracted
          unzip -q "downloads/Nobles-macOS.zip" -d extracted
          ls -la extracted

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create DMG
        run: |
          mkdir -p dist
          create-dmg \
            --volname "Nobles" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --app-drop-link 600 185 \
            "dist/Nobles.dmg" \
            "extracted/Nobles.app"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nobles-dmg
          path: "dist/Nobles.dmg"
